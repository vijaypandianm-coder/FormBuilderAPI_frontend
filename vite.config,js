import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react({ jsxRuntime: 'classic' })], // classic = we import React explicitly to avoid your earlier error
  server: { port: 5173 }
});